spring.application.name=CrowdFunding

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/crowdfunding_db
spring.datasource.username=root
spring.datasource.password=9949

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
# 256-bit (32+ chars). Replace in production via env var / secrets store.
jwt.secret=CHANGE_ME_TO_A_LONG_RANDOM_256_BIT_SECRET_KEY_VALUE
jwt.expiration=86400000

# Swagger UI
springdoc.swagger-ui.path=/swagger-ui.html
